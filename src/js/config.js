// Project configuration
export const API_URL = `https://api.adviceslip.com/advice`;
export const ENABLE_BUTTON_DELAY = 2000;

// DO NOT CHANGE,
// Changing it to a different value causes a bug in Firefox
// That prevents new advice to be fetched from the API
export const CACHING_MODE = 'no-cache';
